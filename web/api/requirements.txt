fastapi==0.115.5
uvicorn[standard]==0.32.0
websockets==13.1
python-multipart==0.0.18
pydantic==2.9.2
# The shared ``redtonomous`` Python package is installed separately by the
# launch scripts and CI workflow (``pip install -e ./python``) so this file
# can be consumed from any working directory.
